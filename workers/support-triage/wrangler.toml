name = "cleanable-support-triage"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[ai]
binding = "AI"

[[r2_buckets]]
binding = "VERIFICATION_MEDIA"
bucket_name = "cleanable-verification-media"

[vars]
ENVIRONMENT = "production"

# Secrets (set via `wrangler secret put`):
# - CLEANABLE_API_KEY
